apiVersion:  networking.k8s.io/v1
kind:  Ingress
metadata:
  name:  ingress-todoui
  labels:
    app:  todoui
spec:  
  ingressClassName:  azure-application-gateway
  rules:
  - host: todoui.techno028.shop
    http:
     paths:
     - backend:
         service:
           name:  todo-ui-api
           port: 
            number:  80
       path: /
       pathType:  Prefix
